# These are the things that *might* be implemented later

- `benchstats` exit code by default should be standard exit code (0 success, 1 error). Old mode
returning 1 for significant differences should be activated by a CLI flag

- use narrow font for HTML report
- add JS-based selection highlighter for HTML report

- implement metric values range safeguard to reasonably detect outliers and spoiled benchmarks

- custom timer for `qbench`
